# qnetinfo — placeholder (moved to qni.pages.dev)

This repository no longer holds the Quantum Network Infographics source. The
project moved to **<https://qni.pages.dev>** (hosted on Cloudflare Pages).

What's left here is a single static page that GitHub Pages serves at
<https://crxi.github.io/qnetinfo/> to redirect old bookmarks to the new home.
The repo stays **public** only so that GitHub Pages keeps serving that
redirect.

It is a temporary placeholder and will be removed once the old URL has stopped
receiving traffic. Nothing here is maintained — please update any links to
point at <https://qni.pages.dev>.
